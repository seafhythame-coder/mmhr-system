# 🚀 دليل البدء السريع - نظام MMHR

## المتطلبات الوحيدة
- [Node.js](https://nodejs.org/) (الإصدار 16 أو أحدث)
- توكن بوت تليجرام من [@BotFather](https://t.me/BotFather)

---

## ⚡ التشغيل في 3 خطوات

### الخطوة 1: تثبيت المكتبات
```bash
npm install
```

### الخطوة 2: إعداد ملف الإعدادات
افتح ملف `.env` وضع فيه توكن البوت الخاص بك:

```env
TELEGRAM_BOT_TOKEN=ضع_توكن_البوت_هنا
```

> إذا لم يكن لديك ملف `.env`، انسخ `.env.example`:
> ```bash
> cp .env.example .env
> ```

### الخطوة 3: تشغيل البوت
```bash
npm run telegram
```

---

## 🎯 الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `npm run telegram` | تشغيل بوت تليجرام فقط |
| `npm start` | تشغيل السيرفر الرئيسي فقط |
| `npm run bots` | تشغيل كل شيء معاً |
| `npm run setup` | إعداد تلقائي كامل |

---

## 🤖 كيف تحصل على توكن تليجرام؟

1. افتح تليجرام وابحث عن **@BotFather**
2. اكتب `/newbot`
3. اختر اسماً للبوت
4. ستحصل على التوكن - انسخه في ملف `.env`

---

## ✅ ملاحظات مهمة

- **قاعدة البيانات**: تعمل تلقائياً مع SQLite — لا تحتاج تثبيت أي شيء إضافي
- **قاعدة البيانات** تُحفظ في مجلد `data/mmhr.db`
- **الملفات المرفوعة** تُحفظ في مجلد `uploads/`

---

## ❓ مشاكل شائعة

**"Cannot find module 'better-sqlite3'"**
```bash
npm install
```

**"Error: TELEGRAM_BOT_TOKEN is missing"**
→ تأكد من وضع التوكن في ملف `.env`

**البوت لا يرد**
→ تأكد من أن التوكن صحيح وأنك بدأت محادثة مع البوت أولاً
